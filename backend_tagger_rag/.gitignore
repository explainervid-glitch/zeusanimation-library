# Python
__pycache__/
*.pyc
.venv/
venv/

# Model weights (huge — never commit)
models/

# Runtime data / generated
qdrant_db/
input/
*.log

# LLM provider config — CONTAINS API KEYS, never commit
llm_config.json
